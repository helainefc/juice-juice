import React from 'react'
import '../components/Title.css'

export default function Title({ title }) {
    return (
        <div className="secction-title">
            <h4>{title}</h4>
            <div />
        </div>
    )
}
